/* styles.css */

/* Основные стили */
* {
    font-family: 'Roboto', sans-serif;
    box-sizing: border-box; /* Удобнее для работы с отступами */
}

body {
    margin: 0;
    padding: 0;
}

header {
    display: flex;
    justify-content: space-between;
    padding: 20px;
    background-color: #f8f8f8;
}

header a {
    color: black;
    text-decoration: none;
    margin-right: 15px;
}

header a:hover {
    text-decoration: underline;
}

.first-block {
    height: 500px; /* Увеличьте при необходимости */
    background: url('ваше_изображение.jpg') no-repeat right;
    background-size: cover;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
}

h1 {
    margin: 20px 0;
    font-size: 2.5em;
}

h2 {
    margin: 15px 0;
    font-size: 1.5em;
}

p {
    width: 50%;
}

.works {
    display: flex;
    justify-content: space-between;
}

.work-item {
    width: 30%;
    margin: 0 10px;
}

footer {
    display: flex;
    justify-content: space-between;
    padding: 20px;
    background-color: #f8f8f8;
}

.footer-links {
    display: flex;
    flex-direction: column;
}

.popup-container {
    display: none; /* По умолчанию скрыто */
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    justify-content: center;
    align-items: center;
}

form {
    background: white;
    padding: 20px;
    border-radius: 8px;
}

input {
    display: block;
    margin: 10px 0;
    padding: 10px;
    width: 100%;
}

button {
    padding: 10px 15px;
}
